language: elixir
elixir:
  - "1.7"
otp_release:
  - "21.0"

script:
  - mix format --check-formatted
  - mix test

deploy:
  provider: script
  script: bash scripts/deploy-gigalixir.sh
  on:
    branch: master
